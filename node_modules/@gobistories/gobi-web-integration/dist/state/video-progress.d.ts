export declare type VideoProgressListener = (seconds: number) => void;
export declare class VideoProgress {
    private listeners;
    private _seconds;
    get seconds(): number;
    addListener(listener: VideoProgressListener): void;
    removeListener(listener: VideoProgressListener): void;
    update(seconds: number): void;
}
