import 'proxy-polyfill/proxy.min.js';
import { Options, PlayerOptions } from './types/options.types';
declare global {
    const BUILD_HASH: string;
}
export declare class Bubbles {
    private readonly container;
    constructor(config: Options);
}
export declare class Player {
    private readonly container;
    constructor(config: PlayerOptions);
    destroy(): void;
}
interface DiscoverOptions extends Options {
    observerDepth?: number;
    shadowSelector?: string;
    apiBaseUrl?: string;
}
export declare function discover(options?: DiscoverOptions): void;
export {};
