import { Chapter } from '~/types/story.types';
export interface UseAnalytics {
    init: () => void;
    load: () => void;
    play: (chapterIndex?: number) => void;
    pause: (chapterIndex?: number) => void;
    ended: () => void;
    closed: (endTime: number) => void;
    linkClick: (linkUrl: string) => void;
    skipChapter: (endTime: number) => void;
    backToChapter: (endTime: number) => void;
    chapterChange: (chapterIndex: number, end?: boolean) => void;
    newIteration: () => void;
}
declare const useAnalyticsHook: (chapters: Chapter[], analyticsUrl: string, storyTitle?: string, storyId?: string) => UseAnalytics;
export default useAnalyticsHook;
