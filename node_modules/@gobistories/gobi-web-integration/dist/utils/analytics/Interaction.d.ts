import { Tapped, ViewInteraction } from '~/types/analytics.types';
import { Chapter } from '~/types/story.types';
export declare class Interaction {
    chapter: Chapter;
    isTapped: Tapped;
    interactionSent: boolean;
    viewTime: number;
    constructor(chapter: Chapter, isTapped?: Tapped);
    stopTimer(endTime?: number): void;
    complete(endTime?: number): ViewInteraction;
}
