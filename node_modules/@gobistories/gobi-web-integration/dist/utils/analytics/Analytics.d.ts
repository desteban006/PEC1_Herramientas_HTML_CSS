import { Tapped, ViewInteraction } from '~/types/analytics.types';
import { Chapter } from '~/types/story.types';
import { Interaction } from '~/utils/analytics/Interaction';
export declare class Analytics {
    interaction: Interaction;
    wasUserInteraction: boolean;
    completeCount: number;
    private chapters;
    constructor(_chapters: Array<Chapter>);
    createInteraction(chapterIndex: number, isTapped?: Tapped): void;
    completeInteraction(endTime?: number): ViewInteraction | null;
}
