import { StoryOptions } from '~/types/options.types';
import { Story } from '~/types/story.types';
export declare function loadStories(stories: StoryOptions[], useMediaProxy: boolean, apiBaseUrl: string): Promise<Partial<Story>[]>;
